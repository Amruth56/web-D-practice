/*Data Types
undefined, null, boolean, string, symbol, number and object */

var myName= "Amruth"  // it cam be used throughout the prog

myName=8

let ourName="hello world" //it will be defined within the scope where we declare that

const pi=3.14   // variable that can never change